# images/ — illustrations, photos, backgrounds

Drop anything visual that isn't a logo/icon/screen here.

Things Claude will likely need from the design:
- The **partner/company logos** in the "Trusted by" row (Samsung, HelloRache,
  Sitecore, Hiive, Ruby Ribbon, KD, Bending Spoons) — as `.svg` if possible.
- The **referral illustration** (person with phone + coins) on the Pro Plan page.
- Any **hero background** image/gradient, and the grey image placeholder in the
  "Already Breached?" hero (`hero-breached`).

`.svg` for graphics, `.png`/`.jpg` for photos. If unsure, drop it here and tell Claude.
