# screens/ — exported Figma screens (reference)

These are the **source of truth** for pixel-perfect building. Do **not** rename them.

| File | What it is | Where it's used |
|------|-----------|-----------------|
| `home-landing.png` | Full marketing landing page (long scroll) | `/` |
| `hero-passwords.png` | Hero option A — "passwords already on the dark web" (+ scan mockup) | `/` hero (swappable) |
| `hero-breached.png` | Hero option B — "Is your company Already Breached?" | `/` hero (swappable) |
| `report-exposure.png` | Dark credential exposure report | `/report` |
| `dashboard-pro-plan.png` | Dashboard → Pro Plan / Earn Free Credits (referral) | `/app/pro-plan` |
| `dashboard-employee-exposure.png` | Dashboard → Identity Protection → Employee exposure tab | `/app/identity-protection` |
| `dashboard-threat-overview.png` | Dashboard → Identity Protection → Threat overview tab | `/app/identity-protection` |
| `divider-109.png` / `divider-110.png` | Tiny decorative gradient slivers | recreated in CSS |

> Note: the landing page (`home-landing.png`) already contains a third hero
> ("See what's exposed"). All three heroes will be built as swappable components.
